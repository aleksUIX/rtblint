/**
 * rtblint — OpenRTB bid request / response linter
 *
 * ESM entry point. Stub release — full WASM-backed implementation coming in 0.1.0.
 *
 * @module rtblint
 */

/**
 * Validate an OpenRTB JSON payload.
 * @param {string} input - Raw JSON string (bid request or response)
 * @returns {Promise<{valid: boolean, issues: Array}>}
 */
export async function validate(input) {
  void input;
  throw new Error("rtblint 0.0.1 is a stub — full implementation coming in 0.1.0");
}
